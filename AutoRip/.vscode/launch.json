{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "AutoRip Debug",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/Main.ps1",
            "cwd": "${workspaceFolder}"
        }
    ]
}
